# Gateway runtime dependencies.
# This file is used by Dockerfile.gateway. Product-specific services in this
# repository are architecture-spec containers and do not need the historical ML
# dependency bundle in the gateway image.
fastapi==0.141.1
uvicorn[standard]==0.52.1
httpx==0.28.1
